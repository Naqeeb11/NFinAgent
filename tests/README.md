# Test Strategy

Add unit tests for domain rules; contract tests for API/events; integration tests for storage and outbox; replay tests for ledger/backtests; and safety tests for stale data, missing provenance, agent tool permissions, and look-ahead bias.
