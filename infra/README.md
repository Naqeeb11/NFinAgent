# Infrastructure

This directory will hold environment definitions, infrastructure-as-code, observability configuration, backup/restore checks, and policy deployment assets. Local development begins with Docker Compose in `deploy/`.
